/**
 * MoodSelector atom barrel — re-exports from organisms for backward compatibility.
 * The MoodSelector lives in organisms/ as it composes multiple sub-elements,
 * but is re-exported here so App.tsx and other consumers can import from atoms/.
 */
export { MoodSelector } from '../../organisms/MoodSelector'
